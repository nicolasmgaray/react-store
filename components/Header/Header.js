import React from "react";
import { StyledHeader, HeaderText, HeaderImage } from "../../styles/Header";

const Header = () => (
  <StyledHeader>
    <HeaderText>Electronics</HeaderText>
    <HeaderImage></HeaderImage>
  </StyledHeader>
);

export default Header;
